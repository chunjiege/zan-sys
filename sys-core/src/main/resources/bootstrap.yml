eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: sys-server
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        service-id: config-server
#  profiles:
#    active: dev